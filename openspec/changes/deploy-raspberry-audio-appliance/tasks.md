## 1. Retain accepted appliance evidence

- [x] 1.1 Record the current Raspberry Pi 5 8 GB target, coordinated manifest, aggregate preflight, and local product baseline in `deployment/release-manifest.yml`, `deployment/compatibility.yml`, and the 2026-08-25 baseline/preflight acceptance reports.
- [x] 1.2 Establish and verify the dedicated headless PipeWire/WirePlumber service identity, runtime paths, permissions, BlueZ roles, and absence of a competing audio session.
- [x] 1.3 Verify the static/dynamic ownership boundary, named distribution-safe overlays, idempotent managed files, least-privilege modes, authorized diagnostics, and configured network boundary.
- [x] 1.4 Deploy native CamillaDSP and decoder resources with stable identities and verify boot, individual restart, combined restart, audio-session restart, and interrupted-transition recovery using the retained 2026-08-25 reports.
- [x] 1.5 Deploy the authenticated management entry, API, proxy, state services, singleton orchestrator, correlated readiness checks, and visible degraded behavior without making UI acceptance part of deployment closure.
- [x] 1.6 Retain the completed TV input, Bluetooth programme-source, headset takeover, headset fallback, and adaptive-routing hardware evidence in the 2026-08-26 deployment acceptance reports.

## 2. Finish native immutable appliance inputs

- [x] 2.1 Remove active retired audio-protocol compatibility inventory, role branches, templates, handlers, preflight/readiness paths, tests, and current deployment documentation; assert the native stack has no remaining enablement path for them.
- [x] 2.2 Remove active staged-rollout policy selection, local-product checkpoint gates, and controlled-graph deployment allowlists; activate the accepted full runtime only after the existing candidate contract gate passes.
- [ ] 2.3 Consume the immutable Open Cinema, WyrePlumber, management UI, and decoder identities produced by `publish-coordinated-project-releases`, retain the accepted native CamillaDSP artifact, and record every hash and provenance entry in the coordinated manifest.
- [x] 2.4 Make appliance mode reject local-directory, editable, mutable VCS, and unpinned production inputs while preserving an explicit local-source development mode that is reported as mutable and non-release.
- [ ] 2.5 Run installed-version and contract probes for the binding, backend API/schema, UI DTO, decoder protocol and adaptive output, CamillaDSP control contract, and native PipeWire resources; store the installed manifest digest in diagnostics and rollback records.

## 3. Close reproducibility on the current appliance

- [x] 3.1 Retain the existing current-host idempotency, readiness, restart, transition-recovery, authentication, and security evidence without treating it as fresh-image or arbitrary-upgrade qualification.
- [ ] 3.2 Apply the final appliance manifest twice to the provisioned Raspberry Pi 5 8 GB fixture and retain both play recaps, aggregate preflight/readiness output, dynamic-state digests, native-resource checks, security checks, reboot/restart recovery, and a smoke run of the already accepted TV/Bluetooth/headset path.
- [x] 3.3 Update deployment documentation for installation from immutable inputs, development mode, current inventory and supported fixture, network/authentication setup, health, diagnostics, backup, rollback, native processor topology, and the explicitly deferred fresh-install, upgrade, benchmark, and broader-platform campaigns.

## 4. Exercise coordinated rollback and close deployment

- [x] 4.1 Implement transition backups, failure capture, retained manifest identities, safe failed-transition behavior, and coordinated rollback mechanics as recorded in `deployment/acceptance/2026-08-26-coordinated-rollback.md`.
- [ ] 4.2 On the current appliance, capture the previous restorable manifest, transition to the immutable candidate, roll back the coordinated application/UI/binding/decoder/processor/configuration/database boundary, verify readiness, and reapply the candidate.
- [ ] 4.3 Verify graph revisions, endpoint bindings, processor profiles, active revision, permitted audit history, service identity, and native runtime resources after both rollback directions; retain correlated diagnostics, artifacts, backups, irreversible boundaries, and recovery instructions.
- [ ] 4.4 Produce the narrowed deployment acceptance report mapping every requirement to automated output or retained evidence, record the intentionally unsupported campaigns, and close the rollback window only after the current appliance result is accepted.
