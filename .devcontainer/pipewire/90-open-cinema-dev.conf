# Isolated, deterministic endpoints for resolver and WyrePlumber integration.
context.objects = [
  { factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "opencinema.dev.tv"
      node.description = "Open Cinema Dev TV"
      media.class = "Audio/Source"
      audio.position = [ FL FR ]
      open-cinema.fixture = true
    }
  }
  { factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "opencinema.dev.speakers"
      node.description = "Open Cinema Dev Speakers"
      media.class = "Audio/Sink"
      audio.position = [ FL FR ]
      open-cinema.fixture = true
    }
  }
  { factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "opencinema.dev.headset"
      node.description = "Open Cinema Dev Headset"
      media.class = "Audio/Sink"
      audio.position = [ FL FR ]
      open-cinema.fixture = true
    }
  }
]
